<%= form_for(@imagen_evento) do |f| %>
  <% if @imagen_evento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@imagen_evento.errors.count, "error") %> prohibited this imagen_evento from being saved:</h2>

      <ul>
      <% @imagen_evento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cod_evento %><br />
    <%= f.number_field :cod_evento %>
  </div>
  <div class="field">
    <%= f.label :cod_imagen %><br />
    <%= f.number_field :cod_imagen %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
